name: SwiftLint

on:  
  workflow_call:

jobs:
  SwiftLint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: SwiftLint Action
        uses: raphaelbussa/swiftlint-action@v1.0.0
            